#include <iostream>
#include <string>
#include <vector>
using namespace std;

class matrix;
bool comapre(const matrix &, const matrix &);

auto change_val(int, vector<int>);
int main() {
  return 0;
}